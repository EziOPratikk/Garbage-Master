import 'package:flutter/material.dart';

class ApiSetup extends StatelessWidget {
  const ApiSetup({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("API SETUP"),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
